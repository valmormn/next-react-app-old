import { createGlobalStyle } from 'styled-components'

const GlobalStyled = createGlobalStyle`
  html {
		color: #111;
    background-color: #333;
    background-image: linear-gradient(to bottom right, #b8f7ab, #32da78);
    height: 100vh
  }
`

export default GlobalStyled
